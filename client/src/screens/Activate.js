//rsc
import React, {useState} from 'react';
import axios from 'axios';

const Activate = ({ match }) => {

    return (
        <div>
            <h1>Activate</h1>

        </div>
    );
};

export default Activate;
